spring.application.name=YardFlow


# datasource
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
# Dados de conexï¿½o com o banco H2
spring.datasource.url=jdbc:sqlserver://sqlserver-rm554556.database.windows.net:1433;database=yardflowdb;user=admsql@sqlserver-rm554556;password={your_password_here};encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=admsql
spring.datasource.password=Fiap@2tdsvms


# Configuração para mostrar o SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialeto do SQL Server
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# Não recriar o banco a cada inicialização
spring.jpa.hibernate.ddl-auto=none

# Exibir SQL gerado pelo Hibernate
spring.jpa.properties.hibernate.show_sql=true


spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-migration-naming=true
spring.jpa.properties.hibernate.default_schema=dbo
spring.flyway.locations=classpath:db/migration

# thymeleaf
spring.thymeleaf.prefix=classpath:/template/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false

